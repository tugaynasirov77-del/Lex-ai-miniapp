name: scout-niche-weekly
on:
  schedule:
    - cron: "0 7 * * 1"
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS -H "x-cron-secret: ${{ secrets.CRON_SECRET }}" https://lex-ai-miniapp.vercel.app/api/cron/scout-niche || true
